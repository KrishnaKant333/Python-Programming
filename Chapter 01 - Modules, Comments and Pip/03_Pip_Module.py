import pyjokes

joke = pyjokes.get_joke()

print(joke)

# pyjoke is a module which we can install using pip in the terminal and import it to use its functions
# to install it : pip install pyjokes
# in a broader sense : pip install module_name eg. flask, django, numpy, pandas

# module is a file containing code which can be written by someone else(usually) and imported and used in our program
# there are built in module and external module

# pip is the package manager for python. You can use pip to install a module on your system

# We can use python in the terminal by simply typing python and it will go into the REPL (Read evaluate print loop).
